@model IEnumerable<Web_Dev_Proj_Gym.Models.SuperAdmin>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SMpassword)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SMpassword)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SMUserName">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SMUserName">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SMUserName">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
