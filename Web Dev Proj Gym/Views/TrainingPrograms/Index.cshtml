@model IEnumerable<Web_Dev_Proj_Gym.Models.TrainingProgram>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProgramDuration)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProgramPurpose)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.WeightGoal)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ProgramDuration)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProgramPurpose)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.WeightGoal)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
